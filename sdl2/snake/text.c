#include "text.h"

int split(int num) {
	

	return arr;
} 


void printNum(Game* game, int x, int y, int num) {

	int len = 0;
	for (int n=num; n>0; n/=10) len++;

	int arr[len]; len = 0;
	for (int n=num; n>0; n/=10) {
		arr[len] = n%10;
		len++;
	}

	for (int i=0; i<len; ++i) {

	}

}